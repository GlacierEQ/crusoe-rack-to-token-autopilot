[build-system]
requires = ["setuptools>=75", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "crusoe-rack-to-token-autopilot"
version = "1.1.0"
description = "Rack-constrained inference-plan optimization plus telemetry-driven useful-token allocation"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
rack-to-token-autopilot = "rack_to_token_cli:main"
rack-to-token = "rack_token_cli:main"
rack-to-token-fleet = "rack_token_cli:main"
rack-to-token-prometheus = "prometheus_rack_cli:main"

[tool.setuptools]
package-dir = {"" = "src"}
py-modules = [
  "rack_to_token_autopilot",
  "rack_to_token_cli",
  "rack_token_controller",
  "rack_token_cli",
  "autopilot_loop",
  "prometheus_adapter",
  "prometheus_rack_cli",
  "promotion_authority",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
